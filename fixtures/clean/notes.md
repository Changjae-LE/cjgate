# Clean fixture

Plain project notes with nothing sensitive in them. Used to confirm that a
repository with no secrets produces a CJGate **PASS**.

- Build with `npm run build`.
- The gate is evaluated locally; no network access is required.
